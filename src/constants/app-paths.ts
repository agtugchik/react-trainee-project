const enum AppPaths {
  ROOT = '/',
  MAIN = '/main',
  AUTH = '/auth',
  SIGNUP = '/auth/sign-up',
  PROFILE = '/profile',
  IMAGE = '/image',
  IMAGES = '/images',
  NOT_FOUND = '*',
}

export default AppPaths;
